# Generic protocol artifact

`session-logging-protocol.md` is an exact, generic protocol for a research situation in which the AI system is both a studied phenomenon and a data-collection instrument.

It supplies parallel logging streams, coupling-failure tags, review checkpoints, and an audit-trail export structure. It contains no portfolio subject's raw session data.
