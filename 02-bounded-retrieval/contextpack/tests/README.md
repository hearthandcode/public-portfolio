# ContextPack test artifact

`test_contextpack.py` is the exact unit-test source for the copied ContextPack proof. It tests allowlist selection, heading and line-range preservation, deterministic vector normalization, loopback-only validation, and source-hash drift detection.

It is evidence for the bounded proof behavior. It is not an integration or production performance test.
